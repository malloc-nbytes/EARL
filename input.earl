fn test_accessor_bug() {
    let lst = [1,2,3,4,5];

    let x = 1 + lst.nth(1);
    assert(x == 3);

    let y = lst.nth(1) + 1;
    assert(y == 3);

    print("x: ", x, " y: ", y);
}

test_accessor_bug();
