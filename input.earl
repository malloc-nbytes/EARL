mod Main

@pub class Point [x, y, name] {

    @pub x = x;
    @pub y = y;
    @pub name = name;
    desc = "A point class";

    @pub fn set_x(n) {
        x = n;
    }

    @pub fn set_y(n) {
        y = n;
    }
}

let p = Point(1, 2, "My Point");

print("done");
