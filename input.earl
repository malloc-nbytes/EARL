import "other.earl"

mod Main

# let p = Other.Point(1, 2, "my point");

# print(p.get_y());
# p.set_y(10);
# print(p.get_y());

let p2 = Other.Point(3, 4, "my point 2");
print(p2.get_y());
p2.set_y(10);
print(p2.get_y());
