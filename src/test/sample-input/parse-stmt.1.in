# File for test-parsers/test_parsers_parse_stmt
# Tests to make sure the parser can differentiate between
# different types of statements.

let i = 1;
let j = 2;

fn func() {
    print(1);
}

fn func(a) {}

fn func(a, b) {
    print(a, b);
    print(a);
    print(b);
}

func();
