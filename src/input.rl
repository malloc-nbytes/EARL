module Main

#-- Name: sum
#-- Parameter: a: int
#-- Parameter: b: int
#-- Description:
#--   Takes two integers `a` and `b`
#--   and returns the sum.
fn sum(a, b) {
    return a+b;
}

#-- My single variable.
let x = 1;

#-- My double variable a and b.
let a, b = (1, 2);
