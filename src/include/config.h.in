#pragma once

#define PREFIX "@CMAKE_INSTALL_PREFIX@"
#define VERSION "@PROJECT_VERSION@"
#define COMPILER_INFO "@COMPILER_INFO@"
@PORTABLE_DEFINE@

#if defined(_WIN32) && !defined(PORTABLE)
#define PORTABLE
#endif
