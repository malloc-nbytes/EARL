mod Math

@pub
fn max(x, y) {
    if x >= y {
        return x;
    }
    return y;
}

@pub
fn min(x, y) {
    if x <= y {
        return x;
    }
    return y;
}

@pub
fn abs(x) {
    if x < 0 {
        return x * (0-1);
    }
    return x;
}
