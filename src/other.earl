mod Other

fn sum(a, b) {
    let s = a+b;
    return s;
}

class Point [x, y, name] {
    x = x;
    y = y;
    name = name;

    fn test() { return 'a'; }

    fn get_x() {
        return x;
    }
}

