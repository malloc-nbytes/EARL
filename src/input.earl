mod Main

class Point [x, y, name] {
    @pub x = x;
    @pub y = y;
    @pub name = name;
    desc = "a point class";

    @pub fn get_desc() {
        return desc;
    }
}

let p = Point(1, 2, "my point");
