module Main

class Point [x, y] {
    @pub let x: int, y: int = (x, y);
}

let p: Point = Point(3, 2);

let x: int = 1;
let a: int, b: str, c: type = (1, "hi", int);

println(f"{x} {a} {b} {c}");
println(p);
