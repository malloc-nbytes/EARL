mod Main

import "std/list.earl"
import "std/set.earl"

let s = Set.T([10,2,5,22,4,33], |a, b| {
    if a > b { return 2; }
    if b > a { return 1; }
    return 0;
});

let lst = s.extract().filter(|a| { a % 2 == 0; });

List.quicksort(lst, |a, b| {
    return a < b;
});

println(lst);

s.dump();

