mod Main

import "std/set.earl"

let s = Set::T([], |x, y| {
    if x > y { return 2; }
    if y > x { return 1; }
    return 0;
});

s.insert(9);
s.insert(3);
s.insert(1);
s.insert(10);
s.insert(12);

s.sdump();
