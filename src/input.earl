mod Main

import "other.earl"

class Test [x, y] {
    x = x;
    y = y;
    desc = "a description";

    fn get_x() {
        return x;
    }

    fn set_x(dx) {
        x = dx;
    }

    fn get_y() {
        return y;
    }

    fn set_y(dy) {
        y = dy;
    }

    fn get_desc() {
        return desc;
    }
}

let a = 1;
let t = Test(a, 2);
