mod Main

class Node [v] {
    _v = v;
}

fn sum(a, b) { return a+b; }

class T [x, y] {
    _x = x;
    _y = y;
    _c = 'a';

    fn constructor() {
        let n = Node(this._x, this._y);
        let s = sum(1,2);
        println(s);
    }

    fn test() {
        println(sum(1,2));
    }
}

let t = T(1, 2);
t.test();
