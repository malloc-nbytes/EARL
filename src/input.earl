mod Main

import "other.earl"

class Test [x, y] {
    x = x;
    y = y;
    desc = "a description";

    fn test() {println("here");}
    fn get_char() {return 'b';}
    fn get_x() {return x;}
    fn set_x(dx) {x = dx;}
    fn get_y() {return y;}
    fn set_y(dy) {y = dy;}
    fn get_desc() {return desc;}
}

let t = Test(1, 2);

let copy = t.get_x();
println("before: ", t.get_x());

copy = 99;
println("after: ", t.get_x());
