module Main

class Point [x, y] {
    @pub x = x;
    @pub y = y;

    @pub fn get_sum() {
        return this.x + this.y;
    }

    fn priv() {
        println("should not print");
    }
}

fn sum(a, b) {
    return a+b;
}

