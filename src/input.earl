mod Main

import "std/set.earl"

let s = Set::T([], |x, y| {
    if x > y { return 0; }
    if y > x { return 1; }
    return 2;
});

s.insert(3);
s.insert(1);
s.insert(9);
s.insert(5);
s.sdump();

# let cl = |x| {
#     let y = x;
#     if y == 5 { return x; }
#     return cl(y+1);
# };

# println(cl(0));

