mod Main

class T [x, y] {
    _x = x;
    _y = y;
    _c = 'a';

    fn constructor() {
        println(this._x);
        println(this._y);
        println(this._c);
    }

    fn set_x(dx) {
        this._x = dx;
    }
}

let t = T(1, 2);
t.set_x(99);
println(t._x);
