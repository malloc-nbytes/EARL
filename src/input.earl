mod Main

# import "std/set.earl"
#
# let s = Set.T([], |x, y| {
#     if x > y { return 2; }
#     if y > x { return 1; }
#     return 0;
# });
#
# s.insert(3);

let f = |x| { return true; };

match f(1) {
    true -> { println("here"); }
    _ -> { println("should not print"); }
}
