mod Main

import "std/list.earl"

fn test(x) {
    let save = 0;

    for i in 0..100 {
        match i {
            5 -> { save = i; break; }
            _ -> {}
        }
    }
    println("end of function ", save);
}

test(1);
